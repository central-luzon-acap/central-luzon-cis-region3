## /pdf/templates

This directory contains `.ejs` template files used as input for generating HTML strings with [**EJS**](https://ejs.co/).

HTML `.html` file counterparts of each `.ejs` file display how the template will look like, using placeholder values.

@ciatph
20220723
