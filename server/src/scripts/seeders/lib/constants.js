const CONSTANT_STATIC_COLLECTION = 'constant_data'

const DOCUMENTS = {
  // List of AMIA vilages with lat/lon data and other info
  AMIA_VILLAGES: 'amia_villages',
  ASSETS_OPENGRAPH: 'opengraph'
}

module.exports = {
  CONSTANT_STATIC_COLLECTION,
  DOCUMENTS
}
