import Typography from '@mui/material/Typography'

function SuperAdmin () {
  return (
    <div>
      <Typography variant='h4'>
        ACAP Super Admin
      </Typography>

      <p>Welcome to ACAP Super Admin, a set of administrative pages for managing ACAP admin accounts and other data-specific content which needs higher-elevated account access in the future.</p>
      <p>It currently allows User Management under the <b>Users</b> tab.</p>
    </div>
  )
}

export default SuperAdmin
