import { Utilities } from './utilities'

const UTILS = new Utilities()

export const getPageAssetsDoc = UTILS.getPageAssetsDoc.bind(UTILS)
export const getTyphoonAdvisory = UTILS.getTyphoonAdvisory.bind(UTILS)
export const getPdfDownloadURL = UTILS.getPdfDownloadURL.bind(UTILS)
